[db]
    # Default is dbengine, which writes metrics to disk. Keeping them in RAM
    # means this container needs no volume at all.
    mode = ram

[web]
    # Already the default in this version; stated explicitly so the port nginx
    # proxies to is visible in our own configuration file.
    bind to = *
    default port = 19999
